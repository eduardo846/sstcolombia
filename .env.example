# Copia este archivo como .env y cambia TODOS los valores antes de producción.
POSTGRES_DB=sgsst
POSTGRES_USER=postgres
POSTGRES_PASSWORD=cambia-esta-clave-de-superusuario
AUTHENTICATOR_PASSWORD=cambia-esta-clave-de-postgrest
# Mínimo 32 caracteres. Genera uno con: openssl rand -base64 48
JWT_SECRET=cambia-este-secreto-de-al-menos-32-caracteres
